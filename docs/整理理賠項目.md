# 任務規劃：整理理賠項目 (Claim Items) 子詞庫化

本文件旨在規劃將名詞定義已驗證成功之 **Method 1 (CLASSIFIED - 險種分類)** 機制推廣至理賠項目（`claim_item`）的任務內容與開發時程估計。
---

## 2. 理賠項目分類依據

理賠項目將與名詞定義保持相同的 5 大險種分類邊界，並使用既有的關鍵字規則引擎與 LLM 進行語義判定：

| 險種分類 | 英文代號 | 核心判定特徵 / 白話邏輯 |
| :--- | :--- | :--- |
| **通用型** | `general` | 不論險種，所有保單皆可能出現的通用或法律給付（如：身故保險金、完全失能保險金、解約金、保險費退還）。 |
| **健康保險型** | `health` | 圍繞疾病、醫療行為、住院、手術、健保、癌症、特定傷病、長期照顧、生理/認知功能障礙等理賠給付。 |
| **傷害意外保險型**| `injury` | 強調「外來、突發、非疾病」的意外傷害事故所導致的傷害、骨折、意外身故或意外住院等給付。 |
| **投資型保險型** | `investment`| 涉及金融市場、專設帳戶配置、資產評價等具備投資性質的理賠或給付項目。 |
| **壽險與年金保險型**| `life_annuity`| 圍繞生存與壽險返還、生存保險金、滿期保險金、定期生存年金、保證年金給付等項目。 |

> [!IMPORTANT]
> **資料 SSOT 原則**：在執行首次分類拆分時，我們**直接讀取並運用 `base_claim_items.json` 中現有的 40 多個既存項目內容**，僅對其進行險種標記與拆分，**不透過 LLM 重新生成或重寫表達式**，以確保既存精算邏輯`logic_structure` 與 `parameters` 10

| 開發與測試環節 | 預估耗時 | 用途 | 具體作法 |
| :--- | :--- | :--- | :--- |
| ~~**1. 基底詞庫修復與首次拆分**~~ | **1 天** | 將現有單一的 `base_claim_items.json`（45 筆）依照 5 大險種邊界做**無損分類拆分**，確保 Pipeline 在雙層對齊時能正確載入對應的主要子庫與備用子庫，同時保留既存 `logic_structure` 與 `parameters` 表達式完整不變。 | - 執行 `src/classify_claim_items.py` 套用規則引擎 + Gemini LLM 語意分類器，將所有理賠項目分入 `general / health / injury / life_annuity / investment` 五個子 JSON<br>- 執行 `scratch/analyze_keys.py` 掃描是否有斷鍵變數，並以 `scratch/find_name_mismatch.py` 掃描命名空間不一致的代號（如 `INSURANCE_AMOUNT` → `SUM_INSURED`）<br>- 驗證五個子 JSON 總筆數與原始 JSON 完全一致（無遺漏、無重複） |
| ~~**2. 批次語義聚類與去重工具開發**~~ | **1.5 天** | 處理 160+ 商品解析後產生的**大量碎片化 `NEW_GENERAL` 理賠項目**：先大批量收集候選，再透過 Embedding 語意相似度將白話描述不同但精算邏輯等價的給付項目自動收斂，最終形成數十個可直接晉升的「標準精算給付群組」，大幅減少後續的人工逐筆審查成本。 | **候選收集 (0.5 天)**：<br>- 開發批次掃描腳本，一次性遍歷 `data/claim_items/products/*.json`，收集所有 `classification == "NEW_GENERAL"` 的理賠項目，輸出帶商品來源的候選清單<br>**語義聚類 (1.0 天)**：<br>- 呼叫 Gemini Embedding API 將每個候選項目的 `display_name + description` 特徵化為向量<br>- 以餘弦相似度 > 0.85 為基準執行 K-Means / 層次聚類，將碎片收斂為語義群組<br>- 輸出每群代表項目與所有成員清單，供後續批次晉升使用 |
| ~~**3. 參數來源自動診斷與標記引擎**~~ | **1.5 天** | 確保理賠項目的 `parameters` 變數來源**可被追溯且命名空間統一**，防止前端精算計算或查表時發生「變數找不到定義」的斷鍵錯誤。 | **三向診斷引擎 (0.75 天)**：<br>- 對每個理賠項目的 `parameters` 自動判定來源類型：<br>  → `DEFINITION_ALIGN`：能在名詞定義庫中找到對應 `code` 的標準變數<br>  → `SYSTEM_DERIVED`：由 Pipeline 計算得出（如 `policy_year`、`elapsed_years`）<br>  → `USER_INPUT`：需由前端使用者手動輸入的外生變數（如 `DAILY_BENEFIT_AMOUNT`）<br>- 將標記結果寫回 JSON 的 `source_type` 欄位<br>**命名一致性靜態檢查 (0.75 天)**：<br>- 整合 `analyze_keys.py` 與 `find_name_mismatch.py` 的掃描邏輯，當偵測到中文概念相同但 code 不同時，自動推薦標準名詞代號並輸出警示報告，杜絕斷鍵隱患 |
| ~~**4. 升級管理端與批次人機審查界面**~~ | **1.0 天** | 讓精算人員能以**「語義群組」為單位**批次覆核並一鍵晉升，而非逐筆審查，將數百次人機問答縮減為數十次群組確認，大幅節省維護時間。 | - 在 `manager.py` 新增 `batch-promote` 子命令，接受群組清單 JSON 作為輸入<br>- 對每個群組顯示：代表項目的完整 `logic_structure`、`parameters`、以及來自多少商品的變體定義<br>- 支援一鍵確認整個群組晉升至對應險種子庫，並自動執行跨子庫防禦性去重與聯動更新商品層 `classification` 狀態 |
| **5. 大批量全自動 Pipeline 驗證** | **1.75 天** | 在生產規模的 160+ 商品上端到端驗證整個系統的正確性，確保備用庫 fallback 對齊、資訊融合、以及 `NEW_GENERAL` 比例降幅均達到預期指標。 | - 批次執行 `src/run_pipeline.py` 處理所有 160+ 商品 PDF<br>- 逐一抽查輸出 JSON，驗證：備用庫命中並正確融合、`EXISTING_MATCH` 比例顯著上升、`NEW_GENERAL` 比例下降 90% 以上<br>- 對剩餘的 `NEW_GENERAL` 項目執行第二輪人工確認，確認其確為全新給付邏輯後晉升至基底詞庫<br>- 更新 `check_classification.py` 抽樣覆核，確認整體分類一致率 ≥ 95% |ethod 2 處理流 | 0.5 天 | 移除實驗性的 SINGLE_FILE 動態單檔檢索策略，讓 Pipeline 僅保留已驗證效能最佳的 CLASSIFIED 分類子詞庫機制，降低維護複雜度。 | - 刪除 `src/compile_to_single_files.py` 與 `scratch/test_strategies.py`<br>- 移除 `orchestrator.py` 中 `strategy == "SINGLE_FILE"` 整個條件分支<br>- 移除 `run_pipeline.py` 中的 `--strategy` CLI 參數<br>- 執行 `pytest tests/test_classification.py` 確認分類核心未受影響 |

> [!NOTE]
> 以上實作方式並非完全理想, 若實際上不可用, 則再討論其他可行方案. 參閱 [開發保費試算.md](./開發保費試算.md)